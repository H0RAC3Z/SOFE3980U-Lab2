Clean(Remove target directory) and Build(Regenerate and build target directory artifacts)
```
mvn clean install
```
Run Tests and Spring Boot App
```
mvn test spring-boot:run
```
Run just Spring Boot App
```
mvn spring-boot:run
```
Run just Tests
```
mvn test
```
